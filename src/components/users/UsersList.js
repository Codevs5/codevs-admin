import React, { PropTypes } from 'react';

import Header from '../layout/Header.js';

const UsersList = () => (
  <div className="container">
    <Header title="User list" />
    <h1> Lista de usuarios por implementar</h1>
  </div>
);

// UsersList.propTypes = {
//
// };

export default UsersList;
